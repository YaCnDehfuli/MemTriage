# --- Web / API ---
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.34.0
python-multipart==0.0.31
pydantic==2.10.4
pydantic-settings==2.7.1
sse-starlette==2.1.3

# --- Async jobs ---
celery==5.4.0
redis==5.2.1

# --- Persistence ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

# --- Forensics component (wrapped, installed from local path in Docker) ---
# volmemlyzer is installed via `pip install -e components/volmemlyzer` in the
# worker image. It declares volatility3 without a version, so left alone the
# engine silently changes between image builds; pin it here so a run is
# reproducible and a regression is attributable.
volatility3==2.28.0

# --- Rendering / numerics (VADViT preprocessing reproduction) ---
numpy==1.26.4
Pillow==12.3.0
opencv-python-headless==4.10.0.84
# pandas/tqdm are pulled in transitively by VolMemLyzer; declared here because
# the VADViT grid-rendering fidelity test imports VADViT's ProcessVisulaizer.
pandas==2.2.3
tqdm==4.67.1

# --- ML inference (worker image only; kept here for a single source of truth) ---
# torch/torchvision/timm are heavy and CPU-only for the demo; pinned in the
# worker Dockerfile to match VADViT (torch 2.x, timm 0.6.13 — not 0.6.12, see
# the Dockerfile comment: 0.6.12 doesn't import under Python 3.11).

# --- Low-level region analysis (phase 2 deep-dive) ---
# Both are pure-Python-installable and imported lazily: without them the deep-dive
# drops the instruction listing / PE header panels and says so, rather than failing.
capstone==5.0.8
pefile==2024.8.26

# --- Assistant (phase 3, bring-your-own-key) ---
# Both SDKs are optional and imported lazily: a missing one degrades to "install
# this to use that provider", never to a failed request for another provider.
anthropic==0.69.0
openai==1.99.1

# --- Reporting ---
jinja2==3.1.6

# --- Dev / test ---
pytest==9.0.3
httpx==0.28.1
